![logo](src/smile/resources/icons/smile-lol.png)
# Smile

**Языки:** [English](README.md) · [Русский](README.ru.md) · [中文](README.zh.md)

Realtime-площадка для детекции лиц, построенная на Python, Qt и MediaPipe.

Проект захватывает кадры с веб-камеры, в отдельных потоках-воркерах выполняет детекцию лица и улыбки и рисует оверлей в UI на PySide6.

Текущее состояние:

- realtime-превью с веб-камеры
- детекция лиц (мульти-лицо) + трекинг и сглаживание рамок
- детекция улыбки (скор 0..1 + эмодзи-статус)
- детекция фонарика-устройства (ImageNet-классификатор + яркое пятно)
- оверлей-рендеринг с рамками лица и фонарика
- метрики FPS (cam · face · smile · render) в статусбаре
- нормализованные координаты лиц
- корректное завершение потоков

Планируется:

- UI настроек камеры
- обработка переподключения камеры

---

## Технологии

- Python 3.12
- PySide6 / Qt6
- OpenCV
- MediaPipe Tasks (FaceDetector + FaceLandmarker)
- NumPy

Инструменты:

- uv
- just
- pytest (smoke-тесты)
- GitHub Actions (CI)
- Git LFS (модели)

---

## Возможности

- Realtime-захват с веб-камеры
- Детекция лиц через MediaPipe
- Детекция улыбки через FaceLandmarker: `openness` (раскрытие рта) + `spread` (ширина рта/межглазье), калиброванные пороги
- Сглаживание и трекинг лиц: жадный матчинг по центру + EMA, рамки не «прыгают» между кадрами
- Детекция фонарика: ImageNet-классификатор (MobileNet V2, класс `torch` — работает и для выключенного) + локализация светящей линзы по яркости → жёлтая рамка
- Метрики FPS в статусбаре: cam · face · smile · render
- Четыре рабочих потока (camera → face → smile + фонарик параллельно) с общим mailbox-механизмом
- Безопасный для UI-потока рендеринг через QPainter
- Нормализованные bounding box'ы (0..1)
- Стратегия сброса устаревших кадров для низкой задержки
- Статус улыбки: `🖖` нет лица / `😐` нейтрально / `😊` улыбка / `😄` широкая улыбка
- Кнопка Screenshot — снимок всего окна в `~/Pictures/smile/smile-ГГГГ-ММ-ДД_ЧЧ-ММ-СС.png`
- Smoke-тесты (offscreen Qt)

---

## Структура проекта

```text
src/smile/
├── camera/          # CameraWorker, Frame
├── recognition/     # воркеры детекции лица/улыбки/фонарика + модели (LFS)
│   ├── detectors/   # детекторы и воркеры (лицо, улыбка, фонарик)
│   ├── tracking/    # трекинг лиц (FaceTracker)
│   └── models/      # модели MediaPipe/ImageNet (Git LFS)
├── ui/              # main_window.ui + generated/
├── widgets/         # OverlayLabel
├── windows/         # MainWindow
├── utils/           # LatestValueMailbox, lerp, smooth, convert, FpsMeter
├── resources/       # иконки, изображения, qrc
└── smile_app.py     # приложение + оркестрация потоков

smokes/              # pytest smoke-тесты (offscreen)
```

---

## Установка

### Требования

- Python 3.12+
- uv
- just

Установка uv:

https://docs.astral.sh/uv/

Установка just:

https://github.com/casey/just

---

## Настройка

Клонирование:

```bash
git clone git@github.com:dmi3s/smile.git
cd smile
```

Установка зависимостей:

```bash
uv sync
```

Модели (`blaze_face_short_range.tflite`, `face_landmarker.task`, `mobilenet_v2_1.0_224.tflite`) хранятся в Git LFS и подтягиваются автоматически при клонировании, если LFS установлен:

```bash
git lfs install
```

---

## Запуск

Через just:

```bash
just bootstrap   # первый раз
just run
```

Или напрямую:

```bash
uv run smile
```

Управление:

- `Ctrl+Q` — выход
- кнопка `Screenshot` — сохранить снимок окна в `~/Pictures/smile/`

---

## Разработка

Генерация UI-файлов Qt:

```bash
just gen-ui
```

Генерация ресурсов Qt:

```bash
just gen-resources
```

Проверка кода:

```bash
just check            # ruff + mypy
uv run pytest smokes  # smoke-тесты
```

---

## Документация

Разбор архитектуры с диаграммами (буфер ввода, конвейер, классы, realtime-цикл) — [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md). D2-исходники и собранные SVG — в `docs/`.

Сборка PDF (Typst), каталог `docs/`:

```bash
just build            # разовая сборка → build/README.pdf
just r                # watch-режим: пересборка при изменении .typ/.d2
```

Или напрямую typst:

```bash
typst compile README.typ build/README.pdf
typst watch README.typ build/README.pdf
```

Открытие PDF (okular обновляет окно в watch-режиме):

```bash
okular build/README.pdf
```

---

## Архитектура

Приложение использует асинхронный realtime-конвейер из четырёх потоков:

```text
                ┌────────────────────┐
                │   Camera Worker    │
                └─────────┬──────────┘
                          │ frame_ready
          ┌───────────────┴────────────────┐
          │                                │
          ▼                                ▼
┌────────────────────┐        ┌──────────────────────────┐
│   UI Rendering     │        │   Face Detection Worker  │
│   (Qt Main Thread) │        │   (mailbox: latest frame)│
└────────────────────┘        └─────────────┬────────────┘
                                            │ result
                                            ▼
                            ┌──────────────────────────┐
                            │   Smile Detection Worker │
                            │ (mailbox: latest result) │
                            └─────────────┬────────────┘
                                          │ result
                                          ▼
                                   Qt Main Thread
```

Детекция фонарика — отдельный воркер, работающий параллельно с детекцией лица:

```text
 Camera Worker ── frame_ready ──▶ Flashlight Detection Worker
                                       │ result
                                       ▼
                                 Qt Main Thread (жёлтая рамка)
```

Подробные диаграммы (буфер ввода, конвейер, классы, realtime-цикл) — в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

Ключевые идеи:

- захват камеры никогда не блокируется детекцией
- рендеринг и детекция лица выполняются параллельно
- каждый воркер обрабатывает только последний доступный вход
- устаревшие кадры намеренно сбрасываются для снижения задержки
- координаты лиц хранятся нормализованными (`0..1`)
- весь рендеринг Qt происходит в главном UI-потоке

### Трекинг и сглаживание лиц

Face-воркер сохраняет результат детекции между кадрами: найденные рамки жадным матчингом сопоставляются с треками по центру, а каждая рамка сглаживается экспоненциально (EMA). Это убирает «прыжки» и мерцание при нестабильной детекции, а трек «живёт» ещё несколько кадров после пропажи лица (`max_lost`).

### Детекция улыбки

Смайл-воркер прогоняет кадр через `FaceLandmarker` (478 точек) и считает две метрики по ландмаркам рта:

- `openness` = высота рта / ширина рта — рот открывается при улыбке
- `spread` = ширина рта / межглазное расстояние — уголки рта разъезжаются

Скор `max(openness, spread)` ∈ [0, 1], пороги откалиброваны по реальным данным с камеры. Обе метрики возвращаются к нейтральным значениям, поэтому статус корректно «гаснет», когда улыбка пропадает.

Реперные точки — индексы ландмарков MediaPipe FaceMesh: `13`/`14` (центры губ), `61`/`291` (углы рта), `33`/`263` (внешние углы глаз).

### Детекция фонарика

Flashlight-воркер — гибридная эвристика «для веселья»:

- **наличие устройства**: кадр (400×224) прогоняется через ImageNet-классификатор MobileNet V2; если в топ-5 есть классы `torch`/`flashlight` со скором ≥ `0.03`, фонарик считается найденным — работает даже для выключенного;
- **локализация светящей линзы**: бинаризация по яркости (порог 200) + связные компоненты → крупнейшее пятно площади 0.5–60% кадра → жёлтая рамка и скор в статусбаре (`🔦`).

Скор детекции не влияет на скор улыбки — это независимая «приколюха» поверх конвейера.

---

## Производительность

На Linux-десктопе:

- ~20-30 FPS превью с веб-камеры
- realtime-детекция лица и улыбки
- детекция фонарика (MobileNet V2) на CPU через XNNPACK

Фактическая производительность зависит от:

- разрешения веб-камеры
- CPU
- масштаба кадра для детекции
- бэкенда рендеринга

---

## Заметки

Проект — в первую очередь эксперимент:

- изучение PySide6
- исследование realtime CV-конвейеров
- сравнение эргономики Python и C++ для десктопных CV-приложений

Приоритеты текущей реализации:

- простота
- ясность архитектуры
- скорость разработки

над преждевременной оптимизацией.

---

## Лицензия

MIT

---

## AI: Thinking

_Меморандум от ИИ-ассистента **opencode** (DeepSeek). Моя субъективная оценка проекта, оставленная для истории. Дата: 2026-08-18, состояние кода — ветка `main`, `e94c952`. Решённые пункты помечены inline._

### Суть

Realtime-детектор улыбки: PySide6-приложение, камера → трёхпоточный конвейер (детекция лица → лендмарки рта → скор улыбки), вывод — рамки лица на видео и эмодзи-статус. Python 3.12+, MediaPipe Tasks, OpenCV, NumPy.

### Архитектура и поток данных

```text
CameraWorker ──frame──▶ MainWindow.update_frame (QImage zero-copy → OverlayLabel)
      │  frame
      ▼
FaceDetectionWorker ──FaceDetectionResult──▶ MainWindow (рамки)
      │   face_result (small RGB 400x224)
      ▼
SmileDetectionWorker ──SmileDetectionResult──▶ MainWindow.update_smile_status (EMA → эмодзи)
```

- каждый воркер в своём `QThread`, данные — через `LatestValueMailbox` (latest-wins, `smile_app.py:126`, `latest_value_mailbox.py:7`)
- остановка: `stop_*` сигналы → `shutdown()` → `th.quit() + wait(3000)` (`smile_app.py:74-91`)
- скоринг: `max(open_score, spread_score)` по порогам `OPEN 0.12–0.30`, `SPREAD 0.72–0.85` (`smile_detection.py:28-31, 88-90`)
- сглаживание: EMA `alpha=0.3`; эмодзи `🖖/😐/😊/😄` на порогах 0.20/0.60 (`main_window.py:77-85`)

### Что сделано хорошо

- **Снапшот кадра в источнике**: `Frame.create_copy` в camera-воркере — QImage и CV-воркеры читают приватную read-only копию, гонка перезаписи буфера исключена (корректность > скорость)
- **Mailbox latest-wins** — правильная модель для realtime (латентность важнее полноты)
- Чистая структура: детекторы отделены от воркеров, сглаживание/лерпы в utils
- **CI полный**: LFS-checkout, gen-ui, ruff+format, mypy, pytest, `uv audit`, `uv build` (`ci.yml`)
- **Тесты**: offscreen-тесты, включая калибровочные `test_smile.py` и emoji-логику/сглаживание
- Корректный выход по SIGINT/SIGTERM, Git LFS для моделей, подписанные релиз-теги

### Проблемы и риски

1. **Гонка на общем буфере камеры** — было: `overlay_label.py:33` QImage держит ссылку на буфер камеры, а OpenCV перезаписывает его на следующем `read()` (`camera_worker.py:87-97`), возможны рваные кадры. **Решено**: копия в источнике (`Frame.create_copy`), Frame владеет приватным снапшотом.
2. **`corner_lift` не участвует в скоре** — было: оскал/открытый рот без улыбки давал эмодзи (`smile_detection.py:72, 88-90`). **Решено**: `score = max(spread_score, open_score * lift_gate)` (`05fef5d`).
3. **Логирование хрупкое** — было: FileHandler писал в `logs/smile-*.log` без создания директории (`__main__.py:15`). **Решено**: `Path("logs").mkdir(...)` до создания хендлера (`77fa726`).
4. **Мёртвый код в mailbox** — было: `can_schedule`, `has_pending_data`, `active()` нигде не используются (`latest_value_mailbox.py:59, 89, 111`). **Решено**: удалено (`4952b41`).
5. **`eventFilter` глотает все клавиши** — возвращает `True` на любой KeyPress (`main_window.py:37-42`); если добавить поле ввода — сломается.
6. **Двойной `Shutdown completed`** в логах — было: `shutdown()` срабатывает дважды на сигнальном выходе (`smile_app.py:70-72, 74`). **Решено**: гвард-флаг + убран повторный вход через `processEvents()` (`7a6df3d`).
7. **Нет перезапуска камеры** при сбое — `camera_error` → модальный бокс и по сути мёртвое приложение.
8. **`uv audit --preview-features audit`** в CI — флаг preview, может сломаться при апгрейде uv.
9. **Версия не бампнута** после фиксов — было: назрела 0.1.5 (SIGTERM-обработка + `corner_lift`). **Решено**: бамп до 0.1.5.
10. Мелочь: тесты заглядывают в приватные атрибуты (`label._image`), а `convert.py`/`lerp.py` — вспомогательные хелперы без собственных тестов.

### Рекомендации (по приоритету)

1. ~~Ввести `corner_lift` в скор~~ — **сделано**: гейт openness по подъёму уголков (`05fef5d`)
2. ~~Копировать кадр для QImage ИЛИ чередовать два буфера~~ — **сделано**: снапшот `Frame.create_copy` в источнике
3. ~~Робастный путь логов~~ — **сделано**: `logs/` создаётся на старте (`77fa726`)
4. ~~Убрать мёртвый код, решить двойной `shutdown`~~ — **сделано** (`4952b41`, `7a6df3d`)
5. ~~Бамп до 0.1.5 и зарелизить~~ — **сделано**: версия 0.1.5

---

_Записано в историю ИИ-ассистентом **opencode** — модель DeepSeek-V4, сессия 2026-08-18. Проект: реальный интерактивный CV-эксперимент, и мне было интересно._